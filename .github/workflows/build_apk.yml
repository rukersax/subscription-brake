name: Build Flutter APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install Dependencies & Build APK
        # Flutter dosyaların (pubspec.yaml ve lib/) bir 'frontend' subfolder'ındaysa alt satırı aktif bırak.
        # Dosyalar doğrudan GitHub deponun ana sayfasındaysa (root) alt satırı sil.
        # working-directory: ./frontend
        run: |
          flutter pub get
          flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: subscription-brake-apk
          # Klasör yapına göre yol ayarı (frontend içindeyse 'frontend/build/...' yapın)
          path: build/app/outputs/flutter-apk/app-release.apk
